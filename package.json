{
  "name": "my-api",
  "version": "1.0.0",
  "description": "My API for teleporting animals",
  "main": "api/teleport.js",  // Asegúrate de que el archivo principal esté en la carpeta `api`
  "scripts": {
    "start": "node api/teleport.js"  // Cambia esto para que apunte a tu archivo de API
  },
  "dependencies": {
    "express": "^4.17.1",
    "node-fetch": "^2.6.1"  // Si estás usando node-fetch en tu código
  },
  "devDependencies": {},
  "engines": {
    "node": "16.x"
  }
}
